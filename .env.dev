# ECR Deployment Configuration for MariaDB MCP Server
# Copy this file to .env and fill in your AWS credentials

# ── AWS Credentials ──────────────────────────────────────────────────────────
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here
AWS_REGION=ap-southeast-1
AWS_ACCOUNT_ID=your_account_id_here

# ── Docker Image Configuration ───────────────────────────────────────────────
LOCAL_IMAGE_NAME=mariadb-mcp-server
REPO_NAME=mariadb-mcp-server
BUILD_TAG=latest
# ── Build Tag Examples ───────────────────────────────────────────────────────
# Use version from pyproject.toml: BUILD_TAG=0.2.3
# Use git commit hash: BUILD_TAG=$(git rev-parse --short HEAD)
# Use timestamp: BUILD_TAG=$(date +%Y%m%d-%H%M%S)
# Use semantic versioning: BUILD_TAG=v1.0.0 
DB_HOST=159.65.3.13
DB_USER=choochet
DB_PASSWORD=cPzWmoX@46QiYL4HoYVC
DB_PORT=3306
DB_NAME=sprinklecrm
MCP_READ_ONLY=true
MCP_MAX_POOL_SIZE=10
SERVER_BASEPATH=/mcp